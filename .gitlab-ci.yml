stages:          # Define stages of your workflow
  - build
  - test
  - deploy

# Job for building the project
build_job:
  stage: build
  script:
    - echo "Building the project..."
    - # Add your build commands here, e.g., mvn build, npm install, etc.

    # Job for testing the project
test_job:
  stage: test
  script:
    - echo "Running tests..."
    - # Add your test commands here, e.g., mvn test, npm test, etc.

    # Job for deploying the project
deploy_job:
  stage: deploy
  script:
    - echo "Deploying the project..."
    - # Add your deploy commands here, e.g., docker deploy, kubectl apply, etc.
  only:
    - master  # Ensure deployment happens only on the 'master' branch
